# Lua AOC Development Commands

# Show available commands
default:
    @just --list

# Build the solution (via nix build)
build:
    nix build

# Run the solution (via nix run)
run:
    nix run

# Run tests
test:
    echo "Lua testing would go here"

# Clean build artifacts
clean:
    rm -f result

# Lint code
lint:
    lua -e "print('Lua syntax check would go here')"

# Format code
format:
    echo "Lua formatting would go here"
