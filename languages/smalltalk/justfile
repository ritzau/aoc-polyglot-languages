# Smalltalk AOC Development Commands

# Show available commands
default:
    @just --list

# Build the solution (via nix build)
build:
    nix build

# Run the solution (via nix run)
run:
    nix run

# Run tests
test:
    echo "Smalltalk testing would go here"

# Clean build artifacts
clean:
    rm -f result

# Lint code
lint:
    echo "Smalltalk linting would go here"

# Format code
format:
    echo "Smalltalk formatting would go here"
