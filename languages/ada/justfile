# Ada AOC Development Commands

# Show available commands
default:
    @just --list

# Build the solution (via nix build)
build:
    nix build

# Run the solution (via nix run)
run:
    nix run

# Run tests
test:
    echo "Ada testing would go here"

# Clean build artifacts
clean:
    rm -f *.ali *.o *.exe hello
    rm -f result

# Lint code
lint:
    echo "Ada linting would go here"

# Format code
format:
    echo "Ada formatting would go here"
