# C AOC Development Commands

# Show available commands
default:
    @just --list

# Build the solution
build:
    make

# Run the solution
run:
    make run

# Run tests
test:
    make test

# Clean build artifacts
clean:
    make clean

# Lint code
lint:
    make lint

# Format code
format:
    make format
