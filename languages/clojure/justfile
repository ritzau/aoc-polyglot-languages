# Clojure AOC Development Commands

# Show available commands
default:
    @just --list

# Build the solution (via nix build)
build:
    nix build

# Run the solution (via nix run)
run:
    nix run

# Run tests
test:
    echo "Clojure testing would go here"

# Clean build artifacts
clean:
    rm -rf .cpcache target
    rm -f result

# Lint code
lint:
    echo "Clojure linting would go here"

# Format code
format:
    echo "Clojure formatting would go here"
