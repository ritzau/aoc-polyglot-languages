# Tcl AOC Development Commands

# Show available commands
default:
    @just --list

# Build the solution (via nix build)
build:
    nix build

# Run the solution (via nix run)
run:
    nix run

# Run tests
test:
    echo "Tcl testing would go here"

# Clean build artifacts
clean:
    rm -f *.o *.exe
    rm -f result

# Lint code
lint:
    echo "Tcl linting would go here"

# Format code
format:
    echo "Tcl formatting would go here"
