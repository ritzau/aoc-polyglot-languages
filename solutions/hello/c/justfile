# Hello World C
# Import all standard C development commands
import '../../../languages/c/justfile'

# Hello-specific tasks

# Run and show output clearly
hello:
    @echo "=== Hello World C ==="
    @just run

# Verify the output is correct
verify:
    @echo "Verifying hello world output..."
    @just build > /dev/null 2>&1
    @output=$(./hello-c 2>/dev/null) && \
    if echo "$output" | grep -q "Hello"; then \
        echo "✅ Output contains Hello: $output"; \
    else \
        echo "❌ Unexpected output: $output"; \
        exit 1; \
    fi
